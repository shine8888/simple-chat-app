import styled from 'styled-components';
import EmojiPicker from 'emoji-picker-react';

export const EmojisWrapper = styled.div`
  width: 100%;
  max-height: 100%;
  overflow-y: auto;
  background-color: #fff;
`;

export const EmojisPickerWrapper = styled(EmojiPicker)``;
